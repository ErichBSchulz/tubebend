{
  "name": "erich",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "http-server -p 8000",
    "watch": "browser-sync start --proxy 'localhost:8000' --files '**/*.html, **/*.js, **/*.css'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^1.3.7",
    "http-server": "^14.1.1"
  }
}
